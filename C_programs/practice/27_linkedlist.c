#include<stdio.h>

struct node{
	int a;
	struct node *next;
};


struct node * creat(struct node *k)
{
	scanf("%d",&next.a);
	k.next=0;
	return k;
}	


int main()
{
	struct node *head=0,*last=0,*next=0;
	char c='y';
	while(c=='y')
	{
		next=creat(head);
		if(head==0)
			head=next;
		else
			next->=next;
		scanf(" %c",&c);
	}
}

